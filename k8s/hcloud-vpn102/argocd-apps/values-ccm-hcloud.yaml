---
hcloud-cloud-controller-manager:
  env:
    HCLOUD_LOAD_BALANCERS_NETWORK_ZONE:
      value: eu-central

    
  networking:
    enabled: true
    network:
      valueFrom:
        secretKeyRef:
          name: cloud-credentials
          key: network
